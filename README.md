# GlubLM

> *the language model that already forgot this sentence*

**GlubLM** is an 18M-parameter language model that pretends to be a goldfish with a 10-second memory. Inspired by [GuppyLM](https://github.com/arman-bd/guppylm) by Arman BD and by Ted Lasso's meditation on the goldfish as "the happiest animal on earth", GlubLM has a hard 48-token context window - it *cannot* remember what was just said.

Unlike its inspiration, GlubLM uses modern transformer components (RoPE, SwiGLU, RMSNorm) and was trained on a dataset generated by a team of Claude agents - not hand-authored templates.

## Quick start

```bash
# Clone and install from source (not yet on PyPI)
git clone https://github.com/Den-Sec/glublm.git
cd glublm
pip install -e .

# Chat with a trained checkpoint
glublm chat --ckpt checkpoints/glublm_60k_15ep.pt --tokenizer checkpoints/tokenizer_60k.json --prompt "hello"
```

## Architecture

- 18,357,696 parameters (18.4M), 8 transformer blocks
- Hidden dimension 448, 7 attention heads, head dimension 64
- RoPE position encoding, SwiGLU FFN, RMSNorm, weight-tied LM head
- 48-token hard context window (physical 10-second memory)
- BPE tokenizer (HuggingFace tokenizers), vocab 5120
- Trained on 60K samples (49% goldfish / 51% ted lasso wisdom) generated by a multi-agent Claude team
- Test perplexity: 12.14 (on 6083 held-out samples)
- Training: 15 epochs, AdamW, cosine schedule with linear warmup, BF16 on RTX 3060

## Training from scratch

```bash
# Train on the included dataset
glublm train --data data/glublm_60k.json --out checkpoints/my_model.pt --tokenizer-out checkpoints/my_tok.json --epochs 15 --batch-size 64 --lr 3e-4
```

## License

AGPL-3.0 - see [`LICENSE`](LICENSE).

## Credits

- [GuppyLM](https://github.com/arman-bd/guppylm) by Arman BD - the inspiration
- Ted Lasso - "be a goldfish" philosophy
- Anthropic Claude - dataset generation agent team
